# Theme app extension configuration.
#
# Deliberately minimal: Shopify's docs state that "a theme app extension's
# behavior is driven by its app blocks, app embed blocks, and their schema -- so
# the configuration file itself is minimal". `name` and `type` are the only
# required keys; `handle` is optional and Shopify CLI may add one.
# https://shopify.dev/docs/apps/build/online-store/theme-app-extensions/configuration

name = "TackQuote B2B"
type = "theme"
uid = "58696385-69a5-52ca-1f1f-05d7455f9e32401c43d3"
